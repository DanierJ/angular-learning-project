export class Ingridient {
  /*public name: string;
  public amount: number;

  constructor(name: string, amount: number) {
    this.name = name;
    this.amount = amount;
  } The shortcut bellow also do the trick*/
  constructor(public name: string, public amount: number) {}
}
